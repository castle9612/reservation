# Frontend Public Assets

브랜드 로고를 바꾸려면 이 폴더에 `brand-logo.png` 파일을 넣으면 됩니다.

- 사용 경로: `/brand-logo.png`
- 화면 위치: 좌측 상단 헤더 로고
- 관련 코드:
  - `src/main/frontend/src/App.jsx`
  - `src/main/frontend/src/styles.css`

원하는 로고로 교체할 때는 기존 `brand-logo.png` 파일만 같은 이름으로 덮어쓰면 됩니다.
