# Local development
DB_USERNAME=root
DB_PASSWORD=1234

# Optional for production / docker
DB_URL=jdbc:mysql://localhost:3306/reservation?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
SPRING_PROFILES_ACTIVE=dev
SESSION_COOKIE_SECURE=false
# Use validate in production after schema is prepared. For first-time local setup, update can be used temporarily.
JPA_DDL_AUTO=validate

# Docker Compose app container uses the mysql service name by default.
# Override only when the app container should connect to an external DB.
# APP_DB_URL=jdbc:mysql://mysql:3306/reservation?serverTimezone=Asia/Seoul&characterEncoding=UTF-8

# Frontend branding / location (optional)
VITE_STORE_NAME=라본 바디 테라피
VITE_STORE_ADDRESS=서울시 매장 주소를 입력하세요
VITE_STORE_PHONE=010-0000-0000
VITE_KAKAO_MAP_TIMESTAMP=1776303319351
VITE_KAKAO_MAP_KEY=2acjrw73oay7
